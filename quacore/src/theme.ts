import { createTheme } from "@mui/material/styles";

export default createTheme({
  palette: {
    primary: { main: "#f5a325", dark: "#f08205", light: "#f7b145" },
    secondary: { main: "#ffd45c", contrastText: "#fff" },
  },
});
